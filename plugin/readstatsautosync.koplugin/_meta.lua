local _ = require("gettext")
return {
    name = "readstatsautosync",
    fullname = _("Reading statistics auto-sync"),
    description = _("Triggers KOReader's built-in reading statistics cloud sync once per day."),
}
